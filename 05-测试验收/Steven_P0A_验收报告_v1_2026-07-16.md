# Steven P0-A 验收报告 v1

- 日期：2026-07-16
- 基线：`Steven-Phase1-BL-2026-07-16` 与 S2 开发验收成果
- 范围：认证、permission-based RBAC、认证 PostgreSQL schema、环境保护及既有回归
- 结论：P0-A 开发验收通过；PostgreSQL 空库 smoke 因本机镜像不存在而未执行，不代表生产可用

## 自动化结果

```text
25 passed, 1 StarletteDeprecationWarning
```

25 项包括原 Phase 1/S2 规则与新增 P0-A 测试，覆盖：

- 未认证、无效、过期、撤销、注销 Session 401。
- 安全 Session Cookie 属性。
- 伪造 `X-Role/X-Actor` 不改变权限。
- Steven / 审批人 / 管理员权限矩阵。
- 管理员默认不能审批或修改报价。
- 双角色提交人自审阻断与独立审批人处理。
- production/mock、staging/mock、production/demo seed 启动失败。
- 账户创建、角色变更、Session 撤销、账户停用与旧 Session 撤销。
- 原 Phase 1 Dashboard、跨角色和审计读取规则。
- 原 S2 3×5 计算、异常阻断、人工推荐、审批、审计和可编辑追加式 Excel 版本。

## 前端结果

- `pnpm lint:web`：通过。
- `pnpm build:web`：通过。
- production build 包含 `/login`、`/api/auth/[...path]`、`/api/steven/[...path]`、`/dashboard/steven`、S2 列表和详情路由。

## 实际 HTTP Smoke

| 验证 | 结果 |
|---|---|
| `/health` | 200；development/mock，demo seed 开启，AI 禁用 |
| Steven Dashboard API | 200；身份由服务端 `MOCK_IDENTITY` 生成 |
| FastAPI 伪造身份头 | 400 `legacy_identity_headers_forbidden` |
| Next.js proxy 伪造身份头 | 400 `identity_override_forbidden` |
| Steven 访问管理员账户 API | 403，缺少 `accounts:manage` |
| `/login` | 200 |
| `/dashboard/steven` | 200，采购比价路由存在 |

## Migration

- 链：`20260716_0001 → 20260716_0002 → 20260716_0003`。
- 离线 SQL 命令返回 0。
- 已验证 `users/roles/permissions/user_roles/role_permissions/auth_sessions`。
- 已验证 `uq_auth_sessions_token_hash`。
- Alembic 运行入口读取 `DATABASE_URL`。

本地 PostgreSQL 容器检查：

```text
Error response from daemon: No such image: postgres:16-alpine
```

未拉取镜像、未连接数据库，空库 migration 与 schema smoke 未执行，留待具备已批准本地镜像或数据库环境后补充。

## 证据文件

- `Steven_P0A_pytest_2026-07-16.txt`
- `Steven_P0A_frontend_lint_2026-07-16.txt`
- `Steven_P0A_frontend_build_2026-07-16.txt`
- `Steven_P0A_http_smoke_2026-07-16.json`
- `Steven_P0A_environment_guards_2026-07-16.json`
- `Steven_P0A_migration_offline_2026-07-16.sql`
- `Steven_P0A_postgres_container_check_2026-07-16.txt`
- `Steven_P0A_api_smoke_server_2026-07-16.log`
- `Steven_P0A_web_smoke_server_2026-07-16.log`

## 已知限制

- S2 业务、审批、审计和版本元数据仍为内存实现，重启持久化与事务留待 P0-B。
- 未执行 PostgreSQL 空库、并发、重启恢复和备份恢复测试。
- 未连接 NAS、真实校方账户、真实文件、密钥、DeepSeek、OCR 或外部服务。
- pytest 仅有第三方 TestClient 弃用提示，不影响本轮测试结果；本轮没有为消除警告安装或升级依赖。
