# Steven P0-A.1 安全收口验收报告 v1

- 日期：2026-07-16
- 结论：开发安全收口与回归通过；PostgreSQL 空库 smoke 未执行，不可标记为生产可用

## 自动化结果

```text
35 passed, 1 warning in 15.30s
```

原 25 项 P0-A / Phase 1 / S2 回归全部保留；新增覆盖：

- 首管理员首次创建、重复拒绝、密码不出现在 stdout/stderr。
- Session 写请求缺 CSRF、错误 Token、非法 Origin、缺 Session Cookie 均 403。
- 合法 Session + CSRF + Origin 写请求成功。
- 四种旧身份头在 login 与 FastAPI 业务 API 统一拒绝。
- 登录失败计数、短时锁定、成功清零和 `last_login_at`。
- 进程内登录限流 429 及安全事件。
- Session/CSRF/Origin/权限拒绝安全事件。
- 角色变更撤销旧 Session、自审阻断和职责分离无回归。

唯一 warning 为既有第三方 `StarletteDeprecationWarning`；本轮未安装或升级依赖。

## 前端与 Proxy

- ESLint：通过。
- Next.js production build：通过。
- 构建路由包含 `/login`、`/api/auth/[...path]`、`/api/steven/[...path]`、Dashboard 与 S2 列表/详情。
- 运行时 proxy smoke 共 8 组：四种旧身份头 × login/Steven API，全部返回 400 / `legacy_identity_headers_forbidden`。
- auth/Steven proxy 均转发 Cookie、Origin、Referer 和 `X-CSRF-Token`；多个 `Set-Cookie` 可透传。
- 前端写请求统一使用 CSRF helper，不生成或转发身份覆盖头。

## Migration

- 离线链：`20260716_0001 → 20260716_0002 → 20260716_0003 → 20260716_0004`。
- `0001/0002/0003` 未修改。
- 已验证离线 SQL 包含 `uq_auth_sessions_token_hash`、`auth_security_events`、结果状态检查和两个事件查询索引。
- 新增空库验证脚本具备专用 smoke 库名保护及显式重建开关。

## PostgreSQL 实际验证状态

执行本机镜像检查得到：

```text
Error response from daemon: No such image: postgres:16-alpine
```

本轮未拉取镜像、未安装 PostgreSQL、未连接真实数据库、未修改系统服务。空库创建、在线 migration、实际 bootstrap、数据库重启恢复与 schema smoke 待批准环境具备后执行。

当前既有 `.venv` 未安装 `requirements.txt` 中声明的 PostgreSQL driver；本轮按限制未安装。空库脚本会延迟检查该 driver 并提供明确错误，实际执行时须使用已批准且已按项目依赖清单准备的隔离环境。

## 证据索引

- `Steven_P0A1_pytest_2026-07-16.txt`
- `Steven_P0A1_frontend_lint_2026-07-16.txt`
- `Steven_P0A1_frontend_build_2026-07-16.txt`
- `Steven_P0A1_proxy_legacy_headers_smoke_2026-07-16.json`
- `Steven_P0A1_migration_offline_2026-07-16.txt`
- `Steven_P0A1_postgres_check_2026-07-16.txt`

## 剩余边界

- 多实例共享限流未实施；当前进程内限流仅作本地补充，账户锁定是持久核心防护。
- 首管理员后续生产密码轮换 UI/受控恢复流程未实施，进入生产前必须另行完成并验收。
- S2 业务持久化、事务、并发导出与原子文件发布全部留给 P0-B。
- 未接真实账户、真实校务数据、NAS、DeepSeek、OCR、外部服务或密钥。
