# Steven S2 采购比价验收报告 v1

- 日期：2026-07-16
- 基线：`Steven-Phase1-BL-2026-07-16`
- 范围：仅 S2 采购比价；未进入 S1/S3
- 数据：全部为明确标注的脱敏演示数据

## 完成内容

- Open Design 成功 Run `e6edf2ef-592c-4ff4-96c1-659c4ea4fed6` 已归档，并落地采购列表、详情矩阵、结构化录入、导入预检、异常/附件/审计/版本抽屉、人工推荐、审批与导出确认。
- 实现采购事项、标准品项、供应商、报价明细、导入批次、审批、审计和文件版本的后端合同与内存运行实现。
- 实现数量、非负金额、复合唯一、报价完整性、币种一致性、过期提醒、供应商级固定运费/税费仅计一次等规则。
- 系统最低价排序与人工推荐严格分离；推荐字段种子默认空白。
- 导入采用“预检 → 人工确认写入”；有阻断错误不写入。
- 正式导出只允许人工审批后执行，每次追加新 XLSX 版本，不覆盖历史。

## 标准 3 × 5 计算结果

| 排名 | 供应商 | 品项小计 | 运费 | 税费 | 含税总价 |
|---:|---|---:|---:|---:|---:|
| 1 | 文具供应商丙（脱敏） | 2,393.00 | 90.00 | 100.00 | **2,583.00** |
| 2 | 文具供应商甲（脱敏） | 2,405.00 | 120.00 | 80.00 | **2,605.00** |
| 3 | 文具供应商乙（脱敏） | 2,367.00 | 180.00 | 75.00 | **2,622.00** |

系统最低价为供应商丙；人工推荐仍为空白。税费和运费按供应商级固定金额各计一次。

## 自动化测试

执行：

```powershell
.\.venv\Scripts\python.exe -m pytest apps\api\tests -q
```

结果：`17 passed, 1 StarletteDeprecationWarning`。覆盖：

- 15 条完整报价及三家总价排序。
- 14 条缺报价阻断完整比较，不产生排名。
- 币种不一致阻断比较。
- 过期报价醒目 warning，但不自动选择供应商。
- 数量为 0、负单价、负运费、负税费阻断导入确认。
- 重复供应商+品项报价阻断。
- 非最低价推荐缺理由或审批意见时拒绝。
- 人工提交、不同审批人批准、审计事件。
- 连续导出 v1/v2，不覆盖历史。
- 导出 XLSX 可由 openpyxl 重新打开、修改并另存。
- 跨角色访问 Steven S2 返回 403；Steven 普通角色读取平台全量审计返回 403；事项级审计仅返回当前事项记录。

## 前端验证

- `pnpm lint:web`：通过。
- `pnpm build:web`：通过；动态路由包含 `/dashboard/steven/quotes`、`/dashboard/steven/quotes/[quoteId]`、`/api/steven/[...path]`。
- 实际 HTTP smoke：`/health`、S2 API、列表页和详情页均为 200；跨角色和全量审计均为 403。
- CDP 实际点击“人工推荐”成功，`role=dialog` 对话框出现；不是静态截图或无事件按钮。

## 证据

- 标准样本：`Steven_S2_脱敏标准样本_3家5品项_HKD_v1_2026-07-16.xlsx`
- HTTP smoke：`Steven_S2_http_smoke_2026-07-16.json`
- migration 离线 SQL：`Steven_S2_migration_offline_2026-07-16.sql`
- 实际导出证据：`Steven_S2_Excel导出证据/generated/quotes/demo-quote-hkd-2026/` 下 v1、v2；`Steven_S2_Excel导出证据/人工编辑复核副本_v1.xlsx` 已重新打开、修改并保存
- 列表截图：`Steven_S2_采购事项列表_2026-07-16.png`
- 详情截图：`Steven_S2_采购比价详情_2026-07-16.png`
- 交互截图：`Steven_S2_人工推荐交互_2026-07-16.png`

## 已知限制

- 未连接真实 PostgreSQL；运行数据为进程内存，migration 未应用到真实数据库。
- 未接 DeepSeek、OCR、供应商搜索、真实文件、密钥或外部服务。
- 标准导入确认仅允许写入空白采购事项，避免覆盖/重复；增量合并未实现。
- 报价过期当前为 warning；完整比较仍允许，必须由人工复核。
- 税费暂按供应商级固定金额处理，待校方正式口径后再调整。
- 附件抽屉仅说明留档边界，本阶段不处理真实附件。
- 本地审批身份头和 Next.js proxy 仅用于开发验收，不代表生产 JWT/RBAC。
- Open Design 成功产物已落地；后续 MCP 读取曾返回 `Transport closed`，未修改 daemon 或系统环境，不影响已归档成功 Run。
