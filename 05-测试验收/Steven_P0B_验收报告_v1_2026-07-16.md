# Steven P0-B 验收报告 v1

- 日期：2026-07-16
- 基线：`Steven-Phase1-BL-2026-07-16`、S2、P0-A / P0-A.1
- 范围：S2 PostgreSQL Repository/UoW、事务、重启恢复契约、并发审批/导出与追加式 Excel 文件版本
- 结论：开发实现、离线 schema 和自动化回归通过；真实 PostgreSQL 空库集成 smoke 未执行，不能标记为生产可用或可试运行

## 1. 自动化结果

### 后端全量

```text
45 passed, 1 warning in 24.77s
```

- 既有 35 项 Phase 1 / S2 / P0-A / P0-A.1 回归无失败。
- 新增 10 项 P0-B 事务、并发、版本和环境装配测试。
- 唯一 warning 是既有 Starlette TestClient/httpx 弃用提示；本轮未为消除提示安装或升级依赖。

### P0-B 专项

```text
10 passed in 5.00s
```

专项覆盖：

1. 导入业务写入与审计同时回滚。
2. payload hash 不一致时零行写入。
3. 并发确认仅一个成功，另一个 409；最终 5 品项、3 供应商、15 明细。
4. 双审批决定仅一个成功，另一个 409。
5. 10 路并发导出得到唯一版本 1–10、唯一 storage key、ready 状态、64 位 SHA-256 和正确 size。
6. 导出失败版本为 failed，下一次使用新版本号，不复用旧号，并写失败审计。
7. 文件已发布但数据库登记失败后可从 reserved 对账到 ready。
8. 已发布文件损坏时对账标记 failed，不回收版本号，并写失败审计。
9. staging/production S2 application 不回退内存 Repository。

P0-B + 既有 S2 聚焦套件另行得到 `21 passed, 1 warning`。

## 2. 事务与并发证据

| 验证 | 自动化结果 | 证据性质 |
|---|---|---|
| 导入异常整体回滚 | 通过；品项、供应商、报价、批次确认及审计恢复到事务前 | 内存 UoW 合同测试 |
| payload hash 篡改 | 通过；返回 `import_payload_hash_mismatch`，零行业务数据写入 | 服务/UoW 合同测试 |
| 两路并发导入 | 通过；1 成功 + 1×409，最终 3×5=15 | 线程化内存 UoW 合同测试 |
| 非最低价人工规则 | 既有 S2 回归通过；理由或审批意见缺一不可 | 业务规则回归 |
| 双审批并发 | 通过；1 成功 + 1×409，提交人自审仍为 403 | 线程化内存 UoW 合同测试 |
| 10 路并发导出 | 通过；版本 1–10 唯一且文件元数据一致 | 线程化内存 UoW + 本地文件合同测试 |

上述并发测试证明应用层合同与失败语义，不等同于 PostgreSQL `FOR UPDATE`、部分唯一索引、连接池和进程间并发的真实数据库证明。真实 PostgreSQL 集成脚本已准备，但本轮未执行。

## 3. 重启恢复与文件证据

- PostgreSQL Repository 的事项、品项、供应商、报价、导入批次、推荐、审批、版本和审计读取均来自数据库表；供应商 subtotal/total 在数据库查询时由报价行重新聚合，不依赖进程缓存。
- staging/production app factory 每次应用实例创建后通过相同数据库配置恢复 S2 application；验证脚本包含“创建新 FastAPI app 后重新登录并读取 5/3/15、排名、版本和审计”的真实 PostgreSQL smoke 步骤。
- 真实数据库重启/应用重启恢复**未实际执行**，因为没有获批准 PostgreSQL 空库环境。
- 本地文件合同测试实际重新打开生成的 `.xlsx`，验证可读；10 个 ready 版本的路径不重复、size 与磁盘一致、SHA-256 均为 64 位。
- 文件已发布但 DB ready 登记失败的故障注入测试通过：版本保持 reserved，随后对账为 ready；损坏文件则变为 failed。

## 4. Migration 与受控退出

Migration 链：

```text
20260716_0001 → 20260716_0002 → 20260716_0003 → 20260716_0004 → 20260716_0005
```

离线 SQL 生成成功，已核对包含：

- `payload_sha256`
- `next_export_version`
- `uq_steven_quote_approvals_one_pending`
- `uq_steven_quote_versions_storage_key`
- `steven_quote_audit_events`
- head `20260716_0005`

在清空必要配置后执行两个受控工具：

- `verify_p0b_postgres.py`：exit 2，列出缺少配置；未尝试数据库连接。
- `reconcile_s2_exports.py`：exit 2，列出缺少配置；未尝试文件对账或数据库连接。

## 5. 前端回归

- `pnpm --filter @puiying/web lint`：通过。
- `pnpm --filter @puiying/web build`：通过。
- production build 保留 `/login`、`/dashboard/steven`、`/dashboard/steven/quotes`、`/dashboard/steven/quotes/[quoteId]` 与现有 proxy 路由。

本轮未改变已验收的 Phase 1 页面行为，也未新增 S1/S3 页面。

## 6. PostgreSQL 实际 smoke 状态

**真实 PostgreSQL 空库集成 smoke 未执行。**

外部阻塞：当前未提供获批准的 PostgreSQL 实例、专用凭据、维护库连接和一次性空库环境；当前项目虚拟环境也没有可用 psycopg driver。遵守本轮限制，未安装依赖、未拉取镜像、未连接任何真实数据库、未修改宿主机服务。

因此以下仍是待批准环境执行项：`alembic upgrade head/current`、首管理员 bootstrap、真实 Session/CSRF/RBAC、真实数据库 3×5 导入、应用重启读取、并发导入、双审批、10 路导出、数据库/文件元数据一致性及关键约束/索引检查。

## 7. 证据文件

- `Steven_P0B_pytest_2026-07-16.txt`
- `Steven_P0B_specialized_tests_2026-07-16.txt`
- `Steven_P0B_compile_2026-07-16.txt`
- `Steven_P0B_frontend_lint_2026-07-16.txt`
- `Steven_P0B_frontend_build_2026-07-16.txt`
- `Steven_P0B_controlled_exit_2026-07-16.txt`
- `Steven_P0B_postgres_smoke_status_2026-07-16.txt`
- `Steven_P0B_alembic_offline_2026-07-16.sql`

## 8. 验收结论

P0-B 可标记为“开发实现、离线 migration 与自动化回归通过”。在真实 PostgreSQL 空库集成、历史 actor 治理、备份恢复、NAS/正式存储 Adapter、监控与安全运营条件完成前，不可标记为生产可用、已部署或已进入受控试运行。
