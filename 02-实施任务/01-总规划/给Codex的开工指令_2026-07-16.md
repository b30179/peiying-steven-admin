# 给 Codex 的开工指令

请在 `D:\ZM\AI工坊\某中学\` 开始 Steven 岗位 AI 行政助手项目的 Phase 1。

开始前必须依次阅读：

1. `D:\ZM\AI工坊\某中学\00-项目规范\design.md`
2. `D:\ZM\AI工坊\某中学\01-当前方案\Steven岗位AI实施方案_整合版_v4.6_2026-07-16.docx`
3. `D:\ZM\AI工坊\某中学\02-实施任务\Steven_Codex实施任务书_v3_2026-07-16.md`
4. `D:\ZM\AI工坊\某中学\README.md`

严格以 `design.md` 为最高规范、v4.6 为当前业务基线、v3 任务书为本轮范围。所有新产出只写入 `D:\ZM\AI工坊\某中学\`；不要修改 `D:\ZM\AI工坊\04-项目提案\` 中的历史资料。

本轮只完成任务书中的 Phase 1：建立可运行的项目骨架和 Steven Dashboard，落实后端分层、统一 API/权限/审计边界，以及采购报价明细的 `UNIQUE(quote_supplier_id, quote_item_id)` migration 约束。

前端职责必须分离：页面视觉、信息架构与交互设计先由 Open Design 产出；Codex 负责整理 Open Design 输入 brief、保存设计产物，并将其实现为真正运行的 Next.js 页面。不要自行另起一套视觉设计。若 Open Design 不可用，保留完整阻断证据，继续做不依赖其设计的项目骨架、后端和前端结构，停止在最终视觉实现前。

后续内嵌大模型默认选 DeepSeek，但本轮仍不接入真实 AI/OCR。现在只预留可关闭的后端 Adapter 和人工确认流程；未来只能从 FastAPI 服务端调用 DeepSeek，密钥必须仅存服务端环境变量或受管密钥存储，禁止前端直连、禁止将密钥写入代码、文档或日志。

不得提前实现完整业务，不得部署或改系统环境，不得写入真实数据或密钥。

执行要求：

- 先检查现有目录、环境和可复用代码，再决定初始化方式。
- 使用保守、可回退的最小改动；不要覆盖用户已有文件。
- 每完成一个阶段就运行对应测试或 smoke，不要只创建文件。
- 先将 Open Design brief 写入 `04-开发实现/docs/open-design-steven-dashboard-brief.md`；Open Design 的 artifact、截图或组件说明保存到 `04-开发实现/docs/open-design/`。
- 前后端均实际启动验证；验证 `/dashboard/steven`、后端 health、Steven API envelope、跨角色拒绝和审计事件样例。
- 测试记录、截图和验收证据写入 `05-测试验收/`，本轮变更说明写入 `90-审计记录/`。
- 如果缺少依赖或需要安装大型依赖，先列出准确依赖、用途和影响，停下来询问，不要自行安装。

完成 Phase 1 后停止并汇报：改动文件、启动命令、测试结果、Open Design 产物及其落地对应关系（或阻断证据）、页面地址/截图、已落实约束、已知限制和下一步建议。不要继续进入 S1/S2/S3 完整业务开发。
