# Steven P0-A 实施记录 v1

- 日期：2026-07-16
- 基线：`Steven-Phase1-BL-2026-07-16` 与 S2 脱敏演示 / 开发验收成果
- 范围：P0-A 认证、RBAC 与 PostgreSQL 基础底座

## 决策落实

1. 账户来源采用本地账户 + PostgreSQL 服务端 Session + HttpOnly Cookie；SSO 仅保留未启用适配入口。
2. Steven、审批人、管理员职责分离；管理员默认无审批权限。
3. Session 默认空闲 30 分钟、绝对 8 小时。
4. development/test mock 与 staging/production session 隔离；production 禁止 demo seed。
5. 正式文件存储只保留接口规划，本轮未连接校内 NAS。

## 变更摘要

- 新增环境配置、启动保护、PostgreSQL Engine/Session 工厂和独立环境模板。
- 新增本地账户、密码哈希、服务端 Session、CurrentPrincipal、permission-based RBAC 和管理 API。
- 新增 `20260716_0003`，建立 users、roles、permissions、user_roles、role_permissions、auth_sessions；未修改 `0001/0002`。
- 将 Steven Dashboard 与 S2 API 从角色名 seam 切换为权限 dependency。
- 前端删除身份模拟头，新增登录代理和登录页；FastAPI 与 Next.js proxy 均拒绝身份覆盖头。
- 保留 S2 内存业务 Repository，仅由环境开关控制脱敏 seed；未进入 P0-B。
- 自检时同步 ORM 复合主键/外键与 migration，并让 Alembic 从 `DATABASE_URL` 读取环境连接入口。
- 更新启动文档，移除旧 `X-Role/X-Actor` 使用说明。

## 验证记录

- 后端：25 passed，1 条第三方 Starlette TestClient 弃用提示。
- 前端 lint：通过。
- Next.js production build：通过。
- HTTP smoke：健康检查、Dashboard、登录页与页面均 200；伪造身份头 400；Steven 访问管理员 API 403。
- 环境保护：production/mock、staging/mock、production/demo seed 均抛出 RuntimeError 并阻止启动。
- 自审：同一双角色用户审批自己提交的事项返回 403；独立审批人可处理。
- Migration：离线 SQL 生成成功，链为 `0001 → 0002 → 0003`，六张认证/RBAC 表和 Token hash 唯一约束存在。

## 未执行及原因

Docker daemon 可用，但本机没有 `postgres:16-alpine`：

```text
Error response from daemon: No such image: postgres:16-alpine
```

遵守本轮限制，未联网拉取镜像、未安装依赖、未启动容器、未连接数据库。因此 PostgreSQL 空库 migration/schema smoke 未执行；没有将其记录为已完成。

## 安全与范围确认

- 未写入真实账号、密码、Token、数据库连接、校方资料、真实文件或密钥。
- 未部署、未修改宿主机系统服务、未连接 NAS。
- 未接 DeepSeek、OCR、供应商搜索或其他外部服务。
- 未实施 S1/S3。
- 未实施 P0-B 的 S2 PostgreSQL Repository、导入/推荐/审批事务、版本预留、并发导出和重启恢复。

## 状态结论

P0-A 可标记为“开发实施与现有功能回归完成”，不可标记为生产可用、已部署或已完成 PostgreSQL 业务持久化。下一步如获确认，应进入 P0-B 并在批准的 PostgreSQL 环境补空库、事务、并发、重启和备份恢复验证。
