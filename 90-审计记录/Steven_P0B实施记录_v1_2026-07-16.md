# Steven P0-B 实施记录 v1

- 日期：2026-07-16
- 基线：`Steven-Phase1-BL-2026-07-16`、S2、P0-A / P0-A.1
- 范围：S2 PostgreSQL 持久化、事务、审计、重启恢复架构与并发安全文件版本
- 状态：开发实现与回归通过；真实 PostgreSQL 空库集成 smoke 未执行；非生产可用

## 变更记录

1. 新增 `PostgresQuoteRepository`、`PostgresQuoteAuditRepository` 与 `PostgresQuoteUnitOfWork`；S2 application service 成为 API 的统一入口。
2. staging/production 装配 PostgreSQL S2 application，不回退内存；内存 Repository 只保留 development/test fixture。
3. 导入确认锁定事项和批次，重验状态、payload hash、完整性、币种和可编辑状态；业务写入、批次确认和审计同事务。
4. 推荐从持久数据重算排名，不接受客户端最低价；非最低价人工理由/审批意见规则保留，推荐和审计同事务。
5. 提交、批准、退回使用事项/审批行锁与 pending 部分唯一索引；自审继续阻断，双审批冲突返回 409。
6. 追加 `20260716_0005`，补齐导入 payload、审批关联、持久审计、文件版本状态/元数据、索引、外键与原子版本计数器；未修改 `0001`–`0004`。
7. 正式导出改为“DB 预留 → UUID 临时文件 → Excel 重开/hash/size → put-if-absent 发布 → DB ready 登记”；失败版本不回收。
8. 新增 reserved 文件对账工具，处理“文件已发布但数据库登记失败”；对账不删除、不覆盖文件。
9. 新增受限 PostgreSQL 空库验证脚本，覆盖 migration、bootstrap、认证、3×5、重启、并发导入/审批/导出和文件元数据；缺配置安全退出。

## Migration 记录

```text
20260716_0001
→ 20260716_0002
→ 20260716_0003
→ 20260716_0004
→ 20260716_0005
```

- 离线 SQL 已生成并核对 head 与关键约束/index。
- `UNIQUE(quote_supplier_id, quote_item_id)` 保留。
- 每事项一个 pending 审批由 PostgreSQL 部分唯一索引保证。
- 导出版本号由事项级 `next_export_version` 的原子 `UPDATE ... RETURNING` 分配。
- 历史审批 actor 外键使用 `NOT VALID`；进入受控试运行前必须治理旧 actor 并执行 `VALIDATE CONSTRAINT`。

## 验证记录

- Python compile：通过。
- P0-B 专项：10 passed。
- P0-B + S2 聚焦：21 passed，1 个既有第三方弃用 warning。
- 后端全量：45 passed，原 35 项基线无回归。
- 前端 ESLint：通过。
- Next.js production build：通过。
- Alembic offline SQL：成功，head 为 `20260716_0005`。
- 缺配置退出：两个受控工具均 exit 2，未尝试连接数据库或操作文件。
- 真实 PostgreSQL 空库集成 smoke：未执行；没有获批准实例、专用凭据和空库环境，且遵守限制未安装 driver/PostgreSQL、未拉取镜像、未连接真实数据库。

## 边界与未完成项

- 线程化内存 UoW 测试证明应用合同，不替代 PostgreSQL 行锁、部分唯一索引、多连接/多进程并发的真实验证。
- API 重启读取的真实数据库步骤已写入验证脚本，但未在 PostgreSQL 上执行，不宣称已通过。
- 当前文件存储仅为受控本地追加式实现；未连接 NAS/MinIO，未做正式存储权限、备份、病毒扫描或容量监控。
- 未进入 S1/S3；未接真实校务数据、真实账户、真实文件、密钥、DeepSeek、OCR、供应商搜索或外部服务。
- 受控试运行前仍需真实空库 migration/并发/重启 smoke、历史 actor 治理、备份恢复演练、正式存储 Adapter、监控告警及校方数据治理确认。

## 结论

P0-B 已完成开发实现、离线 schema 与自动化回归收口。当前准确状态为“开发实现与回归通过，真实 PostgreSQL 集成待批准环境验证”，不得表述为生产可用、已部署或已进入试运行。
