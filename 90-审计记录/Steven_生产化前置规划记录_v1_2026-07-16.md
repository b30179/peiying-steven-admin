# Steven 生产化前置规划记录 v1

- 日期：2026-07-16
- 类型：规划审计记录
- 基线：`Steven-Phase1-BL-2026-07-16` 与 S2 脱敏开发验收成果
- 输出：`02-实施任务/Steven_生产化前置改造计划_v1_2026-07-16.md`

## 本轮范围

仅规划以下 P0 前置条件，未实施代码或基础设施变更：

1. `P0-1`：替换 `X-Role` / `X-Actor` 开发身份头，规划数据库 Session Cookie、RBAC、前后端职责、开发 mock 隔离和安全测试。
2. `P0-2`：从内存 Repository 迁移到 PostgreSQL Repository，规划 migration、事务、并发版本、备份恢复、数据隔离和回归测试。

## 主要规划结论

- 当前 S2 只能标记为“脱敏演示 / 开发验收完成”，不能标记生产可用。
- 浏览器认证最小方案采用 PostgreSQL 支持的服务端 Session 与 HttpOnly Cookie；浏览器不保存角色或可伪造身份。
- 后端使用 permission-based RBAC；管理员默认不是审批人，审批角色需显式授予。
- production 禁止 mock 认证和 demo seed；开发、测试、staging、生产使用独立数据库和文件根目录/凭据。
- Service 保留现有业务规则，通过 Repository / Unit of Work 切换到 PostgreSQL。
- 导入确认、推荐、审批和审计在同一事务中完成。
- Excel 导出采用数据库预留版本、临时文件、原子发布、完成事务；失败版本不复用，历史文件不覆盖。
- 现有 migration 链保持不变：`20260716_0001 -> 20260716_0002`；生产化改造只新增后续 revision。
- S1/S2/S3 复用统一 accounts/auth/rbac/audit/approvals/files 底座。

## 未执行事项

本轮未修改业务代码、现有 migration、依赖、数据库配置、密钥或运行环境；未连接 PostgreSQL，未部署，未调用 DeepSeek/OCR 或任何外部服务。

## 待确认事项

- 校方账户来源与 Session/密码政策。
- Steven、审批人、管理员的实际人员映射和是否允许兼任。
- PostgreSQL、正式文件存储、备份恢复指标及运维责任方。
- 审计、审批和正式文件的保留期限。
